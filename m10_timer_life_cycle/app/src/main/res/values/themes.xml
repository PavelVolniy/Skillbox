<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.CountdownTimer" parent="Theme.Material3.DayNight">
        <item name="actionBarStyle">@style/ActionBarStyle</item>
        <item name="android:actionBarStyle">@style/ActionBarStyle</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ActionBarStyle" parent="@style/Widget.AppCompat.Light.ActionBar">
        <item name="android:background">@color/primaryMain</item>
        <item name="background">@color/primaryLight</item>
        <item name="android:titleTextStyle">@style/ActionBarStyle.Text</item>
        <item name="titleTextStyle">@style/ActionBarStyle.Text</item>
    </style>

    <style name="ActionBarStyle.Text" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="Theme.CountdownTimer" parent="Base.Theme.CountdownTimer">
        <item name="colorPrimary">@color/primaryMain</item>
        <item name="colorPrimaryDark">@color/primaryMain</item>
        <item name="colorSecondary">@color/primaryLight</item>
        <item name="android:colorAccent">@color/primaryLight</item>
    </style>
</resources>